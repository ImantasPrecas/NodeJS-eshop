// funkcija kuri skaiciuoja uzdirbtu pinigu kieki

const futureProfit = require("./futureProfit");
const profit = require("./profit");

let = ovrAmount = 0;

function overalProfit(list) {
	let i = 0;

	for (const item of list) {
	}
	return "" + itemOveralProfit;
}

module.exports = overalProfit;
